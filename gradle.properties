# 🔥 OPTIMIZACIONES PARA ZORINOS + RAM LIMITADA (Aspire E5-571G)
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:+UseG1GC -Dfile.encoding=UTF-8
org.gradle.workers.max=2
kotlin.daemon.jvm.options=-Xmx1024m
kotlin.incremental=true
kotlin.incremental.java=true
kotlin.caching.enabled=true

# Desactivar daemon para evitar acumulación de memoria
org.gradle.daemon=false

# Kotlin version (compatible con Room 2.6.1 + KSP)
kotlin.version=1.9.23

# Kotlin daemon
kotlin.daemon.jvm.options=-Xmx1536m

# AndroidX
android.useAndroidX=true
android.enableJetifier=false

# Suppress warnings SDK 34
android.suppressUnsupportedCompileSdk=34
